import React from 'react';
import CounterList from './CounterList';
import {
  StyleSheet,
  Text,
  TouchableOpacity,
  View,
  ScrollView,
} from 'react-native';
import PropTypes from 'prop-types';

const App = () => {};

export default App;
